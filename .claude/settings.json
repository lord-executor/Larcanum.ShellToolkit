{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; f=\"${f%$'\\r'}\"; case \"$f\" in *.cs) dotnet format \"src/ShellToolkit.slnx\" --include \"$f\" --verbosity quiet ;; esac; } 2>/dev/null || true",
            "timeout": 60,
            "statusMessage": "Running dotnet format..."
          }
        ]
      }
    ]
  }
}
